# Issue tracker

This repository uses local Markdown issues under `.scratch/<feature>/`.

Each issue directory should contain a Markdown file describing the work. The issue's front matter should include a title and the applicable triage labels. Local Markdown issues are the source of truth for planned work.
